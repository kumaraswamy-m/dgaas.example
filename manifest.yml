---
applications:
- name:   dgaasx-kmg
  memory: 512M
  path:   target/DGaaSX-2.0.1.war

  # the value of the `host` property needs to be unique across the
  host:   dgaasx-kmg

  # used services

  services:
  - Document Generation-lc
